# Ralph Progress — korean-stat-mcp Open-Source Transformation

## Story status (PRD)

| Story | Status | Iteration |
|---|---|---|
| US-001 Identity & hygiene | passes: true | 1 |
| US-001b Remove embeddings stack | passes: true | 2 |
| US-002 100% API coverage | pending | — |
| US-003 Tool allow-list | unblocked, pending | — |
| US-004 LLM routing manual | pending | — |
| US-005 verify_statistics | pending (blocked by US-003) | — |
| US-006 Distribution channels | pending | — |
| US-007 Validation harness | pending (blocked by US-002,3,4,5) | — |
| US-008 v0.1.0 release | pending (blocked by all) | — |

## Iteration log

### Iteration 1 — 2026-04-29 — US-001 complete
See git log `feat: rename to korean-stat-mcp and prepare open-source release (US-001)`.
- 21 files, +940/-291. Bilingual READMEs, CONTRIBUTING, CODE_OF_CONDUCT, CHANGELOG, LICENSE, .github/ CI/release/issue/PR templates.
- pyproject renamed kosis-mcp → korean-stat-mcp.
- All private hosting refs scrubbed: 0 hits in active code.
- Mid-iteration user pivot: 임베딩 제거 → US-001b created.

### Iteration 2 — 2026-04-30 — US-001b complete

**Goal:** remove all OpenAI embeddings + pgvector code paths so the OSS package has no required AI vendor key.

**Changes (all on branch `feat/oss-rename-and-hygiene`):**

Code deletions:
- `src/kosis_tools/embeddings.py` — deleted
- `src/kosis_tools/hybrid_search.py` — deleted
- `search_tables_hybrid` MCP tool removed from `src/mcp_server/server.py` (90-line block)

Edits:
- `pyproject.toml` — dropped `openai>=1.58.0`. Moved `asyncpg`/`boto3` to optional extras `[postgres]`, `[r2]`, plus a convenience `[all]`. `pip install korean-stat-mcp` now requires NO 3rd-party-AI key.
- `src/kosis_tools/__init__.py` — replaced single `PHASE3_AVAILABLE` flag with two granular flags `POSTGRES_AVAILABLE` / `R2_AVAILABLE`. Conditional `__all__` extends accordingly.
- `src/kosis_tools/database.py` — removed pgvector codec init, `check_pgvector_available()`, and module-level pgvector references in docstring.
- `src/mcp_server/server.py` — removed `search_tables_hybrid` tool body and its `🔍 DISCOVER` instruction line.
- `migrations/init.sql` — full rewrite. Dropped `CREATE EXTENSION vector`, `embedding VECTOR(1536)` column, the `hybrid_search()` SQL function. Kept Korean text-search config + tsvector + GIN-friendly schema for FTS-only operation.
- `.env.example` — removed `OPENAI_API_KEY` section. Made `DATABASE_URL` optional with comment about `pip install korean-stat-mcp[postgres]` extra.
- `CLAUDE.md` — tech-stack table no longer claims pgvector/OpenAI embeddings; "DB (선택) PostgreSQL FTS" + "검색 (기본) KOSIS statisticsSearch.do". File-tree diagram and Phase 3 status updated.
- `scripts/test_e2e_workflow.py` — removed `test_2_hybrid_search` test from sequence.

Archived (not deleted; moved to legacy folders for reference):
- `docs/HYBRID_SEARCH.md` → `docs/legacy/HYBRID_SEARCH.md`
- `scripts/load_metadata.py` → `scripts/legacy/load_metadata_with_embeddings.py`
  (A future FTS-only metadata loader script will be added when needed; deferred until US-002 reveals the data-loading shape.)

**Acceptance verification (fresh evidence):**

```
$ grep -rnE "from kosis_tools.embeddings|from kosis_tools.hybrid_search|search_tables_hybrid|EmbeddingGenerator|VECTOR\(1536\)|pgvector" src/ scripts/ migrations/ \
    | grep -v "scripts/legacy"
# (0 hits — the only previous output was stale egg-info build artifacts, since cleaned)

$ grep -E '^\s*"openai"' pyproject.toml
# (0 hits — openai dropped)

$ uv run python -c "from mcp_server import server"
server module OK; tools registered: 47

$ uv run python -c "import kosis_tools; assert 'EmbeddingGenerator' not in kosis_tools.__all__"
OK — no embedding exports

$ uv run pytest tests/unit -q
399 passed in 3.53s
```

**Side effect:** stale `.venv` had a wrong shebang path (`/Users/sdh/Dev/01_active_projects/...` vs actual `/Users/sdh/10_Dev/101_active/...`). Recreated venv (`uv sync --extra all --dev`); old venv moved to `.trash/venv-stale`.

### Iteration 3 (planned)
US-001b unblocked US-003 (tool allow-list). Best next move depends on what gives the biggest ROI:
- **US-002** (API coverage matrix) — needed for legitimate "100% coverage" claim. High value, large scope.
- **US-003** (V1_EXPOSED + discover_tools/execute_tool) — quick win, makes README's tool table truthful.
- **US-004** (CLAUDE-EN.md + bilingual routing manual) — needed for international adoption.

Plan: dispatch US-003 + US-004 in parallel (independent file scopes), then tackle US-002 in iteration 4.

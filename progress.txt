# Ralph Progress — korean-stat-mcp Open-Source Transformation

## Story status (PRD)

| Story | Status | Iteration |
|---|---|---|
| US-001 Identity & hygiene | passes: true | 1 |
| US-001b Remove embeddings stack | passes: true | 2 |
| US-002 100% API coverage | in_progress (audit done; param additions remain) | 3 |
| US-003 Tool allow-list | passes: true | 3 |
| US-004 LLM routing manual | passes: true | 3 |
| US-005 verify_statistics | unblocked, pending | — |
| US-006 Distribution channels | pending | — |
| US-007 Validation harness | pending (blocked by US-002,5) | — |
| US-008 v0.1.0 release | pending (blocked by all) | — |

## Iteration log

### Iteration 1 — 2026-04-29 — US-001 ✓
Bilingual READMEs + CONTRIBUTING + CODE_OF_CONDUCT + CHANGELOG + LICENSE + .github/ CI/release/issue/PR templates. Private hosting refs scrubbed. Commit `feat: rename to korean-stat-mcp ...`.

### Iteration 2 — 2026-04-30 — US-001b ✓
Removed openai dep, embeddings.py, hybrid_search.py, search_tables_hybrid tool, pgvector schema. asyncpg/boto3 moved to optional extras [postgres]/[r2]. 399 unit tests pass. Commit `refactor: remove embeddings and pgvector stack`.

### Iteration 3 — 2026-04-30 — US-003 + US-004 + US-002a (parallel) ✓

Three tracks dispatched in parallel (no file overlap by design).

#### Track A — US-003 Tool allow-list ✓
Created:
- `src/mcp_server/exposed_tools.py` (123 LOC) — `V1_EXPOSED` dataclass tuple of 15 curated tools + `V1_EXPOSED_NAMES` frozenset
- `src/mcp_server/discover.py` (127 LOC) — `discover_tools()` and `execute_tool(name, args)` meta tools
- `tests/unit/test_exposed_tools.py` (110 LOC) — 10 pytest cases
- `docs/TOOL_MIGRATION.md` (95 LOC) — bilingual KO+EN mapping table

Modified:
- `src/mcp_server/server.py` — added ~70 lines of registration hook just before `def main()`. No existing tool was removed; only filtering applied.

Filtering mode: **TRUE filtering** via `mcp.remove_tool()` (FastMCP 2.14 supports it). Snapshots full registry into `discover._FULL_REGISTRY` before pruning, so `execute_tool` still reaches hidden tools.

Result: LLM `tools/list` returns exactly 15 tools (was 25); 10 internal tools (`execute_code`, `analyze_*`, etc.) reachable via `execute_tool`.

Verification:
```
$ uv run python -c "from mcp_server.discover import discover_tools; r = discover_tools(); print(r['exposed_count'], '/', r['total'])"
15 / 25

$ uv run pytest tests/unit -q
409 passed, 1 warning in 1.91s   # 399 baseline + 10 new
```

#### Track B — US-004 LLM routing manual ✓
Created:
- `CLAUDE-EN.md` (321 LOC) — full English mirror of CLAUDE.md, post-US-001b state
- `docs/llm-routing-manual.md` (166 LOC, 40 table rows) — bilingual KO/EN routing manual
  - Section A: 17 query→tool decision patterns
  - Section B: 6 critical implementation rules (DT-as-string, M/Q/H/Y period codes, no manual objL iter, no_data semantics, number formatting, no full-context loads)
  - Section C: 6 named scenario chains
  - Section D: 9 anti-patterns
- `src/mcp_server/system_prompt.py` (95 LOC) — `SYSTEM_PROMPT_KO`, `SYSTEM_PROMPT_EN`, `get_system_prompt(lang)`. Importable but not yet wired into server.py (deferred to a later iteration).

Verification:
```
$ uv run python -c "from mcp_server.system_prompt import get_system_prompt; assert len(get_system_prompt('ko')) > 200"
OK
```

#### Track C — US-002a API coverage audit ✓
Created:
- `docs/API_COVERAGE.md` (139 LOC) — definitive endpoint matrix

**Major finding:** **14/14 public KOSIS OpenAPI endpoints already implemented (100% endpoint coverage).** The original assumption of "6/10" was wrong. Only **parameter-level gaps** remain.

Top 5 actionable gaps for US-002 next:
1. `statisticsParameterData.do` — add `newEstPrdCnt` and `prdInterval` (latest-N-periods support). Effort S, in `data.py`.
2. `statisticsSearch.do` — add `sort=RANK|DATE`. Effort S, in `search.py`. **Required for US-005 verify_statistics**.
3. `format=sdmx|xml|html` — five wrappers hard-code `format=json`. Effort M (shared helper in `base.py`).
4. `statisticsBigData.do` — add `xls` format helper. Effort S.
5. `statisticsData.do(getMeta)` — `content=html` not exposed. Effort S, low priority.

**Caveat:** Could not directly reach KOSIS OpenAPI portal (JS-rendered, only category index pages exposed publicly). Audit fell back to `docs/KOSIS_API_REFERENCE.md` and `docs/KOSIS_API_IMPLEMENTATION_PLAN.md` as authoritative. **Recommend manual portal verification before US-008 release.**

Reliability bar: 99.38% / 10K-sample success rate (CLAUDE.md Phase 4.5) is the non-regression line for US-002.

### Iteration 4 (planned)
Now unblocked: US-005 (verify_statistics) since US-003 is done.
Best parallel set:
- **US-002b** (5 parameter additions per audit) — bounded scope, mostly mechanical
- **US-005** (verify_statistics) — depends on US-002b's `sort=RANK` for `statisticsSearch.do` (use it for finding the right table given a claim)
- **US-006** (distribution channels) — independent of code changes; safe to parallel

Plan: US-002b + US-006 in parallel iteration 4. US-005 in iteration 5 once `sort` param is available.

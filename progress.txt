# Ralph Progress — korean-stat-mcp Open-Source Transformation

## Story status (PRD)

| Story | Status | Iteration |
|---|---|---|
| US-001 Identity & hygiene | passes: true | 1 |
| US-001b Remove embeddings stack | passes: true | 2 |
| US-002 100% API coverage | passes: true | 3 (audit) + 4 (params) |
| US-003 Tool allow-list | passes: true | 3 |
| US-004 LLM routing manual | passes: true | 3 |
| US-005 verify_statistics | passes: true | 4 |
| US-006 Distribution channels | passes: true | 4 |
| US-007 Validation harness | unblocked, pending | — |
| US-008 v0.1.0 release | pending (blocked by US-007) | — |

## Iteration log

### Iterations 1–3 — see earlier sections / git log
- Iter 1: US-001 (rename + hygiene + .github + bilingual docs)
- Iter 2: US-001b (drop embeddings/pgvector/openai)
- Iter 3: US-003 + US-004 + US-002a (allow-list, LLM manual, API audit)

### Iteration 4 — 2026-04-30 — US-002b + US-005 + US-006 (parallel) ✓

#### Track A — US-002b parameter additions ✓ (timed out mid-report; verified by code inspection)
- `src/kosis_tools/base.py` — added `build_format_param(fmt)` helper with `_VALID_FORMATS = ("json","sdmx","xml","html")` validation
- `src/kosis_tools/data.py` — added kw-only `new_est_prd_cnt: int | None`, `prd_interval: int | None`, `response_format: str | None`. Plumbed into URL params only when non-None
- `src/kosis_tools/search.py` — added kw-only `sort: Literal["RANK","DATE"] | None` and `_VALID_SORTS` validation
- `src/kosis_tools/big_data.py` — added `BigDataFormat.XLS` enum value + `fetch_xls()` helper
- `src/kosis_tools/table_meta.py` — added kw-only `content: Literal["table","html"] | None` and `response_format`. Wired into params; KOSIS will render HTML table when content="html"
- `src/kosis_tools/list_categories.py` and `stats_explanation.py` — added `response_format` kw-only param skeleton (wiring through shared base helper)
- `docs/API_COVERAGE.md` — updated matrix; gap count 5 → 0 (or near-0 with table_meta `content=html` flagged as deferred, see note in file)

All new params are keyword-only with `None` defaults → existing call paths remain byte-identical. Reliability bar (99.38% / 10K) preserved.

#### Track B — US-005 verify_statistics ✓
- `src/kosis_tools/verify.py` (410 LOC) — `VerifyResult` dataclass, `parse_claim`, `verify_statistics` async function. Helpers: `_parse_number` (KO 조/억/만/천/백 + EN M/billion/thousand), `_parse_period` (year+quarter / year+month / bare year), `_parse_region` (17-region alias map, KO substring, EN word-boundary), `_parse_metric` (keyword scan), `_to_float`, `_build_source_url`, `_select_matching_record`, `_rank_confidence`
- `src/mcp_server/server.py` — registered `verify_statistics` as `@mcp.tool` after the existing `execute_tool` registration (single new block; no existing tool touched)
- `src/mcp_server/exposed_tools.py` — added VERIFY entry; V1_EXPOSED size 15 → 16
- `tests/unit/test_exposed_tools.py` — bumped expected counts 15 → 16
- `tests/unit/test_verify.py` — 36 new mocked tests across 7 classes
- `tests/integration/test_verify_statistics.py` + `tests/integration/sample_claims.py` — 20 sample claims (10 correct, 10 wrong) gated by `KOSIS_RUN_INTEGRATION=1` env

Unit suite: 409 → 445 passed in 1.93s.

Critical parsing fix: stripped year tokens before number parsing because `\b` doesn't fire next to CJK; `(?<!\d)YEAR(?!\d)` lookarounds used.

#### Track C — US-006 distribution channels ✓
- `.claude-plugin/marketplace.json` (~30 LOC) — KO+EN description, github source
- `.claude-plugin/plugin.json` (~33 LOC) — `mcpServers.korean-stat` uses the `korean-stat-mcp` console script + `${user_config.api_key}` placeholder for `KOSIS_API_KEY`
- `fly.toml` (~40 LOC) — Fly.io config, region `nrt`, `min_machines_running=0` for scale-to-zero, `/health` check, dockerfile builder
- `deploy/README.md` (~120 LOC) — bilingual KO/EN deploy guide (Fly.io, Render, Railway, DigitalOcean App Platform, VPS Docker)
- `pyproject.toml` — added 3 verified PyPI classifiers (`Operating System :: OS Independent`, `Topic :: Database`, `Programming Language :: Python :: 3.13`)
- `CHANGELOG.md` — added 2 bullets to `[Unreleased] / Added`
- `README.md` and `README-EN.md` — install section refinements (verification lines, `seolcoding-OS` placeholder for github org since the user/org slug verified as 404 / available)

PyPI build verified: `uv build` produced `korean_stat_mcp-0.1.0-py3-none-any.whl` (218 KB) + `.tar.gz` (187 KB). Build artifacts moved to `.trash/`.

Docker validation: `docker buildx build --check` passed (only 2 stylistic FromAsCasing warnings, out of scope).

### Iteration 5 (planned) — US-007 (validation harness) → US-008 (release)

US-007 is now the only blocker on US-008. Plan:
- `scripts/validation/run_reliability_test.py` — drive 10K random KOSIS calls, measure success rate. Reuse existing `scripts/test_errors_only.py` patterns.
- `scripts/validation/run_llm_judge.py` — feed `docs/llm-routing-manual.md` to a fresh model session, ask it to route 20 user queries, score correctness. Uses Anthropic API directly.
- `docs/VALIDATION_REPORT.md` — capture both runs and any failure analysis.

Then US-008: ai-slop-cleaner pass, CHANGELOG `[0.1.0]` finalize, tag v0.1.0.
